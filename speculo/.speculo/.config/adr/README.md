# 工程层 ADR 登记表（EIDON）

> 本目录是**项目工程层 ADR 的唯一权威登记表**。每条 ADR 一个文件、以 `ADR-00XX` 编号作稳定锚点。
> AGENTS.md、CLAUDE.md、`context/CONTEXT.md`、`dev/` 规划与代码注释均以 `(见 ADR-00XX)` 引用本目录。
> **修改对应领域前先读相关 ADR，不要绕过既定边界。**

## 双轨说明（引用须标来源）

- **工程层 ADR** = 本目录，约束代码架构与 EIDON 2.0 数据层落点（机器可强制部分）。
- **产品层 ADR** = `temp/EIDON_数据层架构决策记录_ADR.md` 的十三块基石 `ADR-001~ADR-013` + 五公理 `AX-1~5`，是产品**愿景源**。本期只兑现其「三层【节点】拓扑 + 多模板 schema」子集。
- 两套并存、编号易混（「产品层 ADR-002」vs「工程层 ADR-0002」），**引用必须标注来源轨道**。

## 登记表

| # | 标题 | 轨道 |
|---|---|---|
| 0001 | Core 跑 webview，Rust 能力型编排壳 | 工程基座 |
| 0005 | 完整重构，磁盘契约为集成接缝 | 工程基座 |
| 0006 | Core「内核 + 业务模块」结构 | 工程基座 |
| 0007 | 三层【代码】边界 ESLint 强制 | 工程基座 |
| 0009 | src-tauri 领域文件夹 + 扁平模块 | 工程基座 |
| 0010 | UI 层 = React 19 | 工程基座 |
| **0011** | **EIDON 2.0：以现有系统为基底的颠覆性重构（根决策）** | **EIDON 数据层** |
| 0012 | EIDON 数据层 = core 四模块 + Rust 对称 | EIDON 数据层 |
| 0013 | 固定三层【节点】拓扑 + 深度=层级 + 多模板 | EIDON 数据层 |
| 0014 | EIDON 磁盘契约统一规范化 | EIDON 数据层 |
| 0015 | 版本/diff 直接复用现有 git（不做快照功能） | EIDON 数据层 |
| 0016 | 结构强制 + 违规标记（不自动改写） | EIDON 数据层 |
| 0017 | solomd → EIDON 改名 + `.eidon` 系统区 | EIDON 数据层 |
| 0018 | AI·Agent·Recipes 不在 EIDON 范围内 | EIDON 数据层 |
| **0019** | **物理删除 AI 子系统，core/ai 留接口占位（接替 0018）** | **EIDON 数据层** |
| 0020 | 节点级待办 + 定时提醒 + 通知子系统 | 节点功能 |
| 0021 | 像素宠物「小芽」品牌 / 交互层 | 品牌 |
| **0022** | **完成 solomd→eidon 全量改名（pre-launch 无迁移垫片，扩展 0017、订正 0019）** | **改名收尾** |

> **编号 0002/0003/0004/0008 已删除**：原记录旧 SoloMD 的 AI·Agent·Recipes 子系统内部决策（触发执行、AI 边界、写入策略、MCP），不属 EIDON 产品范围（见 0018），随颠覆性重构删除，编号留空不复用。ADR-0019 接替 0018，将处置方式从「保留不挂载」升级为「物理删除 + 接口占位」。
>
> **注：** AGENTS.md 已于 `refactor/eidon-base` 分支同步更新至与 ADR-0019（物理删除 AI 子系统）一致的现状。
